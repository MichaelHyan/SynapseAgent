# ============================================
# SynapseAgent 依赖清单
# 适用 Python 版本: >=3.10 (开发环境 3.12.6)
# ============================================

# ---------- 核心依赖 ----------
openai>=1.0.0          # LLM 调用 (tools/bot.py: from openai import OpenAI)
colorama>=0.4.6        # 终端彩色输出 (tools/color_utils.py)
mcp>=2.1.1             # Model Context Protocol SDK (tools/mcp_call.py, 需 2.x 的 streamable_http)
httpx>=0.27.0          # mcp 远程/HTTP 传输 (tools/mcp_call.py)
numpy>=1.26.0          # 词向量计算 (tools/wordvec.py)
requests>=2.31.0       # HTTP 请求 (tools/downloader.py, tools/sender.py)
opencv-python>=4.8.0   # 屏幕文字区域检测 (tools/screenshot.py: import cv2)
pyautogui>=0.9.54      # 键鼠模拟 (tools/autogui.py)
pillow>=10.0.0         # 屏幕截图抓取 (tools/screenshot.py: from PIL import Image, ImageGrab)

# ---------- 终端界面 (TUI) ----------
prompt_toolkit>=3.0.0  # SynapseAgentTUI.py

# ---------- Web 界面 ----------
flask>=2.3.0           # web/server.py
flask-socketio>=5.3.0  # web/server.py