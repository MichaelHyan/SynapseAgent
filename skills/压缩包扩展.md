---
name: ziparchive
description: "文件压缩/解压缩规范"
---
# 模块读取要求

了解本模块后回答"已了解模块功能"即可，不要阐述模块功能或直接尝试执行。

# 计时发送技能

本技能定义了如何使用powershell和Compress-Archive压缩/解压缩文件。

## 📋 使用场景

### ✅ 适用情况

当满足以下条件之一时，应启用此技能：

*   **明确请求**：用户明确要求在某事件点需要执行任务。

## 🔧 接口指令

使用终端运行Compress-Archive指令

### 调用示例

仅供参考，不要直接执行。

```bash
# 压缩文件
我将对xxx目录/xxx文件创建压缩文件。<tool_call>powershell Compress-Archive -Path [被压缩文件/目录] -DestinationPath [压缩结果] </tool_call>

# 解压文件
我将解压xxx文件。<tool_call>powershell Expand-Archive -Path [压缩包路径] -DestinationPath [解压目标路径] </tool_call>
```

## 注意事项
- 示例中的文件不存在，仅供参考。读取模块后表明已了解规则即可。
